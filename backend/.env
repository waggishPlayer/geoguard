# Environment Variables
NODE_ENV=development
PORT=4000

# Supabase Database URL with SSL enabled (password has @ so it is URL-encoded)
DATABASE_URL=postgresql://postgres.qsjmcovywdvpjokrgvze:Rockfall@2024@aws-0-ap-south-1.pooler.supabase.com:6543/postgres

# SSL flag for database connection (rejectUnauthorized: false for self-signed)
DB_SSL={"rejectUnauthorized":false}

# JWT Secret
JWT_SECRET=supersecretjwt

# ML Service URL
ML_SERVICE_URL=http://localhost:8000
